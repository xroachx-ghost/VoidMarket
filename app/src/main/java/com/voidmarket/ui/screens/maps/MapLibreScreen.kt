package com.voidmarket.ui.screens.maps

import androidx.compose.foundation.layout.fillMaxSize
import androidx.compose.material.Text
import androidx.compose.runtime.Composable
import androidx.compose.ui.Modifier
import androidx.navigation.NavHostController

@Composable
fun MapLibreScreen(navController: NavHostController, modifier: Modifier = Modifier) {
    // MapLibre integration stub
    // In production, use AndroidView with MapLibre SDK
    Text(
        text = "MapLibre Screen - Dark Theme Map",
        modifier = modifier.fillMaxSize()
    )
}
